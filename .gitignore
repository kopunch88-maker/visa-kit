# Backups (Pack 25 и любые .bak)
*.bak
*.bak[0-9]
*.bak_pre_*
*.before_*

# Patch файлы (временные)
*_PATCH.txt
*_PATCH.ts
*_PATCH.py

# Дамп ФНС (огромный!)
data-*-structure-*.zip
*.zip

# Diag/dump артефакты
*_dump.txt
*_diag.txt
*_diag*.txt
*_results.txt
*_tests.txt
deps_imports.txt
inn_suggest_diag.txt
NPD_CHECK_DAILY.md

# Diag-скрипты, не для прода
backend/diag_*.py
backend/app/scripts/check_schema.py
backend/app/scripts/check_region_code_schema.py
backend/app/scripts/diagnose_*.py
backend/app/scripts/count_modes.py

# PROJECT_STATE копии (оставляем только мастер PROJECT_STATE.md)
PROJECT_STATE_*.md
PROJECT_STATE__*.md
PROJECT_STATE___*.md
PROJECT_STATE — *.md
PROJECT_STATE - *.md
*PROJECT_STATE*копия*.md

# PDF / временные документы в корне
*.md.pdf

# Документация которая не должна быть в репо
ежемесячное_обновление_базы_ИНН.md
ежемесячное_обновление_базы_ИНН.md.pdf

# venv/build
.venv/
__pycache__/
*.pyc

# === Дополнения ===

# КРИТИЧНО: секреты
.env
.env.local
.env.*
backend/.env
backend/.env.*

# Локальные SQLite базы
*.db
*.db.backup
dev.db
backend/dev.db

# Python build artifacts
*.egg-info/
backend/visa_kit_backend.egg-info/

# Patch (TS variant — забыл в первый раз)
*_PATCH.ts

# Pack patcher backups
*.bak_pre_pack30_0*

# === Pack 33.6.2 cleanup - prevent stray files in repo ===

# Local one-shot pack patchers (must NOT be committed)
apply_pack*.ps1
apply_pack*.py
backend/apply_pack*.py
backend/apply_pack*.ps1

# Pack patcher .bak files (broader than the existing 30_0-only pattern)
*.bak_pre_pack*

# Backups of CLAUDE.md / PROJECT_STATE.md
CLAUDE.md.bak*
PROJECT_STATE.md.bak*

# Cleanup launchers (this generator and any future)
cleanup_pack*.ps1

# PATCH-style notes (transient)
**/PATCH_*.md

# Local diag utilities at repo root
local_pool_filler.py
snrip_recon.py

# Random empty text files (Windows "New Text Document.txt" etc.)
*.txt.bak

# Backend cache
backend/_cache/

# Pack-specific deploy staging (local helpers, never commit)
/pack*_deploy/
