# Контекст для Claude Code

Прочитай PROJECT_STATE.md в корне репо — это мастер-документ проекта.
Всегда следуй правилам проекта (Правила 1-55 в PROJECT_STATE).

Особенно важно:
- Правило 5: правки >5 строк — полным файлом для Copy-Item -Force
- Правило 18: перед DROP COLUMN — глобальный grep
- Правило 20: перед SQL — dump схемы таблицы
- Правило 53: после dry-run сразу git push, тесты в проде