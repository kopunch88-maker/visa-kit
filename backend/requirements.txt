# Pack 11 — production dependencies для Railway
# Добавлены: psycopg2-binary (PostgreSQL), boto3 (R2), bcrypt, pyjwt

# === Web framework ===
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
email-validator>=2.1.0

# === Database ===
sqlmodel>=0.0.16
psycopg2-binary>=2.9.9   # PostgreSQL driver для production
sqlalchemy>=2.0.25

# === Auth ===
bcrypt>=4.1.2
pyjwt>=2.8.0

# === Templates engine ===
docxtpl>=0.16.7
python-docx>=1.1.0
jinja2>=3.1.3

# === PDF generation (Pack 9) ===
pypdf>=4.0.0
reportlab>=4.0.0

# === Storage ===
boto3>=1.34.0  # для Cloudflare R2 (S3-compatible)

# === Anthropic API (LLM рекомендации) ===
anthropic>=0.34.0

# === HTTP клиент (если используется в каких-то сервисах) ===
httpx>=0.27.0
